name: CI/CD Pipeline

on: [push]

jobs:
  # ==========================================
  # JOB 1 : QUALITÉ (Lint & Tests)
  # ==========================================
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install Flask pytest ruff

      - name: Run Ruff Linter
        run: ruff check .

      - name: Run Pytest
        run: pytest test_app.py

  # ==========================================
  # JOB 2 : DÉPLOIEMENT NGROK (Dépend de 'quality')
  # ==========================================
  deploy-tunnel:
    needs: quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t flask-app .

      - name: Run Docker Container
        run: docker run -d -p 5000:5000 --name my-running-app flask-app

      - name: Install Ngrok
        run: |
          curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com/ buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt-get update && sudo apt-get install ngrok

      - name: Open Ngrok Tunnel and Expose App
        env:
          NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTHTOKEN }}
        run: |
          ngrok config add-authtoken $NGROK_AUTHTOKEN
          ngrok http 5000 > /dev/null &
          sleep 5 
          PUBLIC_URL=$(curl -s http://localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url')
          
          echo "=========================================================="
          echo " L'APPLICATION FLASK EST ACCESSIBLE PUBLIQUEMENT ICI :"
          echo " $PUBLIC_URL"
          echo "=========================================================="
          
          echo "Le tunnel reste actif pendant 120 secondes..."
          sleep 120
          echo "Temps écoulé. Début de la fermeture propre."

      - name: Clean up resources
        if: always()
        run: |
          echo "Arrêt du conteneur Docker et fermeture de Ngrok..."
          docker stop my-running-app || true
          pkill ngrok || true