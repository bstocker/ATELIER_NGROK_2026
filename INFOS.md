## Développement et Qualité du Code

Ce projet utilise `pytest` pour les tests unitaires et `ruff` pour le linting du code Python. 
Assurez-vous d'avoir installé les dépendances de développement avant de lancer ces commandes.

### Installation des outils de test
```bash
pip install Flask pytest ruff